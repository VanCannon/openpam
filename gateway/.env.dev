# Development Environment Configuration
# This file is for local development only - DO NOT USE IN PRODUCTION

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=openpam
DB_PASSWORD=openpam
DB_NAME=openpam
DB_SSLMODE=disable

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
FRONTEND_URL=http://localhost:3000

# Development Mode - ENABLED for quick testing
DEV_MODE=true

# EntraID/Azure AD Configuration (not needed in dev mode)
ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_REDIRECT_URL=http://localhost:8080/api/v1/auth/callback

# HashiCorp Vault Configuration (not validated in dev mode)
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=dev-root-token
VAULT_ROLE_ID=
VAULT_SECRET_ID=

# Session Configuration
SESSION_SECRET=dev-session-secret-not-secure
SESSION_TIMEOUT=3600s

# Zone Configuration
ZONE_TYPE=hub
ZONE_NAME=development
ZONE_ID=
HUB_ADDRESS=

# Protocol Handlers
GUACD_ADDRESS=localhost:4822
RECORDINGS_PATH=./recordings
